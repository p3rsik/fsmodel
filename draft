byte file with blocks
first block is a superblock, which contains:
  - block size
  - block count
  - timestamps?
then data-blocks bitmap(in-use\free)
then inodes bitmap(in-use\free)
then inode table
  - first inode is always a "/" root directory
then blocks
  - first block in inode is filename
